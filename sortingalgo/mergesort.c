#include<stdio.h>
#include<stdlib.h>
#include<time.h>
//merge to shorted array
void mergetwosortedarray(int* a1,int* a2,int l,int mid,int h){
    if(l<h){
    int i=mid,j=mid+1;
    while(l<=i&&j<=h){
        
    }
    }
}
int main(){

}